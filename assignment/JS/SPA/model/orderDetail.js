/**
 * @author : Nimesh Piyumantha
 * @since : 0.1.0
 **/

/**
 * Class declarations
 */
class orderDetail{
    constructor(orderId,cusId,itemId,qty,total) {
        this.orderId=orderId;
        this.cusId=cusId;
        this.itemId=itemId;
        this.qty=qty;
        this.total=total;
    }
}